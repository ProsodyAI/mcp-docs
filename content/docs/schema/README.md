# Schema

**Source of truth: `website/prisma/schema.prisma`.** Run migrations from the dashboard:

```bash
cd website && npx prisma migrate deploy
```

This folder holds reference SQL (`migrations/`). For the live schema, see `website/prisma/schema.prisma`.
